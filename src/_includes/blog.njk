---
layout: base.njk
---

<section class="poem-section">
  <h1 class="poem-title">{{ title }}</h1>
  <p class="poem-data">
    by {% include "author_url.njk" %} on {{ date }}
  </p>
  <article class="poem-text">{{ content | safe}}</article>

  {% set randomPost = collections.blog | getRandom(page, tags) %}
  {% if randomPost %}
    <div class="random-recommendation">
      <p>
        You may also like: <a href="{{ randomPost.url }}">"{{ randomPost.data.title }}"</a>
      </p>
    </div>
  {% endif %}
</section>
